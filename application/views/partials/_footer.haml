%footer

  - if !should_compile? and logged_in?
    .user
      %i{ :class => 'icon-circle' }
      %a{:href => "#"}
        = user
  - else
    .login
      %i{ :class => 'icon-circle-blank' }
      %a{:href => "#"}
        login

  %a.description{ :href => 'https://github.com/briangonzalez/inkpress', :target => '_blank' }
    = "an Inkpress site"

  / %span.copyright
  / = "© #{Time.now.year.to_s}"
  
  %img.logo{ :src => image_path('user/logo.svg')}